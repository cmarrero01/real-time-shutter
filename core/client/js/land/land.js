/**
 * Land class to show the landing page
 */
Shutter.Land = (function(){

	/**
	 * Register
	 * @param e
	 */
	function signUp(e){
		var $this = $(this);
	}

	/**
	 * Login
	 * @param e
	 */
	function signIn(e){
		var $this = $(this);
	}

	/**
	 * Guests
	 * @param e
	 */
	function guest(e){
		var $this = $(this);
	}

	return {
		signUp:signUp,
		signIn:signIn,
		guest:guest
	};
})();